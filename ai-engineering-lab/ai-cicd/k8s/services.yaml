apiVersion: v1
kind: Service
metadata:
  name: ai-service-stable
spec:
  selector:
    app: ai-service
  ports:
    - port: 80
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service-canary
spec:
  selector:
    app: ai-service
  ports:
    - port: 80
      targetPort: 8000
