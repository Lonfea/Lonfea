name: production-rag-ci

on:
  push:
    paths:
      - "ai-engineering-lab/production-rag/**"
      - ".github/workflows/production-rag-ci.yml"
  pull_request:
    paths:
      - "ai-engineering-lab/production-rag/**"
      - ".github/workflows/production-rag-ci.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ai-engineering-lab/production-rag
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
      - run: python -m pip install --upgrade pip
      - run: pip install -e ".[dev]"
      - run: ruff check .
      - run: pytest -q
